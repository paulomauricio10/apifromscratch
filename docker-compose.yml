services:
#  mysql:
#    image: mysql:8.4
#    container_name: container_mysql
#    restart: unless-stopped
#    environment:
#      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
#      MYSQL_DATABASE: ${MYSQL_DB:-api_db}
#      MYSQL_USER: ${MYSQL_USER:-root}
#      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-root}
#    ports:
#      - "3307:3306"
#    #command: ["mysqld","--default-authentication-plugin=mysql_native_password"]
#    volumes:
#      # Mapeia diretório local (host) para o diretório de dados do MySQL no container
#      - "C:\\sdk\\docker\\volumes\\vol-apifromscratch:/var/lib/mysql"
#    #healthcheck:
#    #  # Verifica disponibilidade executando SELECT 1. Usa socket local para evitar dependência de rede.
    #  test: ["CMD-SHELL", "mysql -u root -p$MYSQL_ROOT_PASSWORD -e 'SELECT 1' || exit 1"]
    #  interval: 15s
    #  timeout: 8s
    #  retries: 15
    #  start_period: 60s
    # healthcheck desativado temporariamente para diagnóstico
    # healthcheck:
    #   test: ["CMD-SHELL", "mysql -u root -p$MYSQL_ROOT_PASSWORD -e 'SELECT 1' || exit 1"]
    #   interval: 15s
    #   timeout: 8s
    #   retries: 15
    #   start_period: 60s

  app:
    build: .
    container_name: container_api_app
    restart: unless-stopped
    depends_on:
      - mysql
       #mysql:
        #condition: service_healthy
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3307
      MYSQL_DB: ${MYSQL_DB:-api_db}
      MYSQL_USER: ${MYSQL_USER:-root}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-root}
    ports:
      - "8080:8080"
    # Em dev pode montar código para hot reload, omitido por simplicidade
    # healthcheck do app temporariamente desativado

volumes: {}
